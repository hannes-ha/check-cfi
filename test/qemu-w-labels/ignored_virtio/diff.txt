diff --git a/.gitignore b/.gitignore
index 61fa39967b..7c15bae377 100644
--- a/.gitignore
+++ b/.gitignore
@@ -20,3 +20,13 @@ GTAGS
 *.swp
 *.patch
 *.gcov
+
+build-cfi-no-debug/
+build-cfi/
+build-no-cfi/
+build-addr-san/
+build-mem-san/
+build-thread-san/
+compile_commands.json
+_codeql_detected_source_root
+build-codeql/
diff --git a/hw/virtio/virtio-bus.c b/hw/virtio/virtio-bus.c
index 896feb37a1..a63b2e73c2 100644
--- a/hw/virtio/virtio-bus.c
+++ b/hw/virtio/virtio-bus.c
@@ -243,6 +243,7 @@ int virtio_bus_start_ioeventfd(VirtioBusState *bus)
     return 0;
 }
 
+__attribute__((no_sanitize("cfi")))
 void virtio_bus_stop_ioeventfd(VirtioBusState *bus)
 {
     VirtIODevice *vdev;
@@ -256,7 +257,18 @@ void virtio_bus_stop_ioeventfd(VirtioBusState *bus)
     if (!bus->ioeventfd_grabbed) {
         vdev = virtio_bus_get_device(bus);
         vdc = VIRTIO_DEVICE_GET_CLASS(vdev);
+
+        asm volatile(
+            "jmp .global_DEBUG_LABEL_START_%=\n"
+            ".global_DEBUG_LABEL_START_%=:\n"
+            :
+        );
         vdc->stop_ioeventfd(vdev);
+        asm volatile(
+            "jmp .global_DEBUG_LABEL_END_%=\n"
+            ".global_DEBUG_LABEL_END_%=:\n"
+            :
+        );
     }
     bus->ioeventfd_started = false;
 }
